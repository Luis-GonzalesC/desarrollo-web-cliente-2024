function suma(){
    let dif = document.getElementsByTagName("div")[0];
    dif.textContent = parseInt(dif.textContent) + 1;
}

function resta(){
    let dif = document.getElementsByTagName("div")[0];
    dif.textContent = parseInt(dif.textContent) - 1;
}